# Copie este arquivo para ".env" e preencha se quiser configurar a chave
# por variável de ambiente em vez da tela de Configurações da UI.
# Criar em: platform.openai.com -> API Keys (nome sugerido: voice-recorder)
OPENAI_API_KEY=
